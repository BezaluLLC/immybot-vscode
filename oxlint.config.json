{
  "extends": ["recommended", "typescript"],
  "ignorePatterns": [
    "dist/**",
    "build/**",
    "node_modules/**"
  ],
  "rules": {
    "no-unused-vars": "warn",
    "no-console": "error"
  }
}
